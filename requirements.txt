# Core Dependencies (Required)
streamlit>=1.30.0
pandas>=2.0.0
plotly>=5.18.0

# Phase 2 Dependencies (Optional - For Semantic Evaluation)
# Install these only if you want to enable AI-powered semantic scoring.
# Note: These are large libraries. Install on a stable internet connection.
sentence-transformers>=2.2.0
scikit-learn>=1.3.0